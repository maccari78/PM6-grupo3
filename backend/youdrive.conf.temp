server {
    listen 80;
    server_name youdrive-api.duckdns.org youdrive-grafana.duckdns.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

